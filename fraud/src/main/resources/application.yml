server:
  port: 8081
spring:
  application:
    name: fraud

  datasource:
    username: 'postgres'
    password: '1085'
    url: jdbc:postgresql://localhost:5433/fraud
  jpa:
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
        format_sql: 'true'
    hibernate:
      ddl-auto: update
      show_sql: 'true'

